import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'global-cap-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
